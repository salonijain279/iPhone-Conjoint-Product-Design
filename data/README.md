# Aggregate model inputs

`aggregate_part_worths.csv` contains the intercept and three level changes used to score the eight product profiles.

No respondent-level survey records, personal information, or original submission files are included. The estimates support reproducible scenario ranking but do not allow the underlying conjoint model to be independently refit.
